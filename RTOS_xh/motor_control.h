#ifndef MOTOR_CONTROL_H
#define MOTOR_CONTROL_H

// #define MOTOR_A_IN1 0
// #define MOTOR_A_IN2 1
// #define MOTOR_A_PWM 2 // left motor
// #define MOTOR_B_IN1 3
// #define MOTOR_B_IN2 4
// #define MOTOR_B_PWM 5 // right motor

// #define DEBOUNCE_TIME_MS 20
// #define MOVE_DURATION_MS 10000
// #define PWM_FREQ 100.0f
// #define MAX_INTEGRAL 100.0f

// // PID controller parameters
// extern float Kp_left = 0.1f;
// extern float Ki_left = 0.01f;
// extern float Kd_left = 0.0000f;

// extern float Kp_right = 0.08f;
// extern float Ki_right = 0.01f;
// extern float Kd_right = 0.0000f;

// extern float duty_cycle = 0.5f; // duty cycle %

// extern float setpoint = 25.0;          // Desired speed (cm/s)
// extern float integral_motor_A = 0.0;   // Integral term for left motor
// extern float integral_motor_B = 0.0;   // Integral term for right motor
// extern float prev_error_motor_A = 0.0; // Previous error for left motor
// extern float prev_error_motor_B = 0.0; // Previous error for right motor

// extern bool button_state = false;          // Debounced button state
// extern uint32_t last_debounce_time_ms = 0; // Timestamp of the last state change

// extern bool is_moving = false;          // Flag to indicate if the car is moving
// extern absolute_time_t move_start_time; // Start time for the move duration

// extern bool turned_right = false;
// extern bool is_speed_control_active = true;

// extern float cumulative_distance = 0.0;

#endif